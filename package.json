{
  "name": "tompscanlan.me",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "cross-env LOCAL_CONTENT=1 astro dev",
    "dev:pds": "astro dev",
    "build": "astro build",
    "build:local": "cross-env LOCAL_CONTENT=1 astro build",
    "check": "npm run build && npm run build:local",
    "hooks:install": "git config core.hooksPath .githooks",
    "preview": "astro preview",
    "astro": "astro",
    "publish:site": "tsx scripts/publish.ts site",
    "publish:doc": "tsx scripts/publish.ts doc",
    "publish:list": "tsx scripts/publish.ts list"
  },
  "dependencies": {
    "@atproto/api": "^0.20.9",
    "astro": "^6.4.4",
    "gray-matter": "^4.0.3",
    "marked": "^18.0.5",
    "tsx": "^4.22.4"
  },
  "devDependencies": {
    "cross-env": "^10.1.0"
  }
}
